import random


"""

choice # 取序列中的一个
random.choices([1, 2, 3, 4, 5], [10, 20, 30, 40, 50], k=5) # 有重复的按权重取 [4, 5, 4, 4, 3]
randint(a,b) [a,b]
random [0,1)
random.randrange(1, 100, 5)  # [1,6,11...]
random.sample([1, 2, 3, 4, 5], 3)  # 不重复随机取3个 [2, 4, 1]
triangular # 随机浮点数 对称分布
uniform # 随机浮点数


"""
